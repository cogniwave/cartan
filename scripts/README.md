# Prerequisites

- Install `make` 
  On windows, using [chocolatey](https://chocolatey.org/install), `choco install make` (must be run with admin permissions)
- Install `inkscape`
  On [windows](https://inkscape-manuals.readthedocs.io/en/latest/installing-on-windows.html)
